name: kinroo
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.0

dependencies:
  flutter:
    sdk: flutter

  # 📌 UI Component & Icons
  cupertino_icons: ^1.0.8
  crystal_navigation_bar: ^1.0.4
  iconly: ^1.0.1
  fl_chart: ^1.0.0
  table_calendar: ^3.2.0

  # 📌 Firebase Services
  firebase_core: ^3.13.1
  firebase_auth: ^5.5.4
  cloud_firestore: ^5.6.8
  google_sign_in: ^6.1.6

  # 📌 State Management (Riverpod)
  flutter_riverpod: ^2.6.1
  riverpod_annotation: ^2.6.1

  # 📌 Utilities (Tools)
  intl_utils: ^2.8.10
  shared_preferences: ^2.5.3
  path_provider: ^2.1.5
  
  # 📌 Media (Camera/Video/Image)
  camera: ^0.11.1
  image_picker: ^1.1.2
  video_player: ^2.9.5
  image: ^4.5.4 

  # 📌 AI & Machine Learning
  # ใช้ tflite_flutter แทนตัวเก่าที่มีปัญหา
  tflite_flutter: ^0.11.0 

dev_dependencies:
  flutter_test:
    sdk: flutter
  
  flutter_lints: ^5.0.0 # แนะนำใช้ 5.0.0 หรือล่าสุดที่ stable
  
  # 📌 Code Generator (จำเป็นสำหรับ Riverpod Annotation)
  build_runner: ^2.4.8
  riverpod_generator: ^2.6.3
  
  # 📌 App Icon Generator
  flutter_launcher_icons: ^0.13.1

# 📌 ตั้งค่าไอคอนแอป
flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/icon/icon.png"
  remove_alpha_ios: true
  remove_alpha_android: true
  adaptive_icon_background: "assets/icon/icon_bg.png"
  adaptive_icon_foreground: "assets/icon/icon_fg.png"
  min_sdk_android: 28

flutter:
  uses-material-design: true

  # 📌 Assets (เรียกเป็นโฟลเดอร์ ประหยัดบรรทัด)
  assets:
    # โฟลเดอร์หลัก
    - assets/icon/
    - assets/videos/
    - assets/models/
    
    # โฟลเดอร์รูปภาพแยกตามหมวดหมู่
    - assets/bmi/
    - assets/food/
    
    # ฟอนต์
    - assets/fonts/

  fonts:
    - family: Kanit
      fonts:
        - asset: assets/fonts/Kanit-Regular.ttf
        - asset: assets/fonts/Kanit-Bold.ttf
          weight: 700
        - asset: assets/fonts/Kanit-Italic.ttf
          style: italic